import { Metadata } from "next";

export const metadata: Metadata = {
	title: "VentureVista - Login",
};

const Login = () => {
	return (
		<>
			<h2>Login Page</h2>
		</>
	);
};

export default Login;
